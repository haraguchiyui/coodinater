<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yui06.coodinater">

    <application
        android:name=".MemoApplication"
        android:allowBackup="true"
        android:icon="@drawable/coordist"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClosetActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".CardBottom" />
        <activity android:name=".randomActivity" />
        <activity android:name=".Closet2Activity" />
        <activity android:name=".Edit2Activity" />
        <activity android:name=".ReEditActivity" />
        <activity android:name=".ReEdit2Activity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>